import 'package:flutter/material.dart';

class FavPecsTab extends StatelessWidget {
  const FavPecsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Fav Pecs"),
    );
  }
}
