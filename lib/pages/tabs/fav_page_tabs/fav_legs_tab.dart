import 'package:flutter/material.dart';

class FavLegsTab extends StatelessWidget {
  const FavLegsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Fav Legs"),
    );
  }
}
