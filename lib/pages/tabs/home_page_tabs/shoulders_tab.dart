import 'package:flutter/material.dart';

class ShouldersTab extends StatelessWidget {
  const ShouldersTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Shoulders"),
    );
  }
}
