import 'package:flutter/material.dart';

class PecsTab extends StatelessWidget {
  const PecsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Pecs"),
    );
  }
}
