import 'package:flutter/material.dart';

class LegsTab extends StatelessWidget {
  const LegsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Legs"),
    );
  }
}
