import 'package:flutter/material.dart';

class LatsTab extends StatelessWidget {
  const LatsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Lats"),
    );
  }
}
